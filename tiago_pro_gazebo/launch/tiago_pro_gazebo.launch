<?xml version="1.0" encoding="UTF-8"?>

<launch>
  <!-- Parameters -->
  <arg name="world"  default="empty"/>  <!-- empty, ... (see ../worlds) -->
  <arg name="gzpose" default="-x 0.0 -y 0.0 -z 0.0 -R 0.0 -P 0.0 -Y 0.0"/>
  <arg name="gui" default="true"/>
  <arg name="debug" default="false"/>
  <arg name="recording" default="false"/>
  <arg name="extra_gazebo_args" default=""/>
  
  <!-- start up world -->
  <include file="$(find pal_gazebo_worlds)/launch/pal_gazebo.launch">
    <arg name="world" value="$(arg world)"/>
    <arg name="gui" value="$(arg gui)"/>
    <arg name="debug" value="$(arg debug)"/>
    <arg name="recording" value="$(arg recording)"/>
    <arg name="extra_gz_model_path" value="$(find tiago_pro_gazebo)/models"/>
    <arg name="extra_gz_resource_path" value="$(find tiago_pro_gazebo)"/>
    <arg name="extra_gazebo_args" value="$(arg extra_gazebo_args)"/>
  </include>

  <!-- spawn robot in simulation -->
  <include file="$(find tiago_pro_gazebo)/launch/tiago_pro_spawn.launch"/>

  <!-- bring up robot -->
  <include file="$(find tiago_pro_gazebo)/launch/tiago_pro_simulation_bringup.launch"/>

</launch>
